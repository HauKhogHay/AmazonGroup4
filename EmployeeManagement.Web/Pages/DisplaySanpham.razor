@inherits DisplaySanphamBase
@if (Sanpham == null)
{
    <div class="spinner"></div>
}
else
{
    
<div class="card">
    <input type="checkbox" checked="@IsSelected" @onchange="CheckBoxChanged" />

    <div class="zoomhinhanh">
        <a href="@($"sanphamdetails/{Sanpham.SanphamId}")"><img class="card-img-top imageThumbnail" src="@Sanpham.PhotoPath" /></a>
    </div>

    <div class="card-header">
        <h2>
            @Sanpham.TenSP <br />
        </h2>
        <div class="vote">
            <span class="fa fa-star checked"></span>
            <span class="fa fa-star checked"></span>
            <span class="fa fa-star checked"></span>
            <span class="fa fa-star checked"></span>
            <span class="fa fa-star"></span>
        </div>
        <h4>
            @Sanpham.GiaSP$
        </h4>
    </div>
    @*
        @if (ShowFooter)
        {


            <div class="card-footer text-center">
                <a href="@($"sanphamdetails/{Sanpham.SanphamId}")"
                   class="btn btn-primary m-1">View</a>

                <a href="@($"editsanpham/{Sanpham.SanphamId}")" class="btn btn-primary m-1">Edit</a>

                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div>

        }*@
</div>
}
